import { providers, BigNumber } from "ethers";
export declare type TexternalWalletProviders = "metamask" | "coinbase" | "walletconnect";
export interface IconnectionDetails {
  address: string;
  networkId: number;
  provider: providers.Web3Provider;
  balance: BigNumber;
}
